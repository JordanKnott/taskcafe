query findTask($taskID: UUID!) {
  findTask(input: {taskID: $taskID}) {
    id
    name
    description
    position
    taskGroup {
      id
    }
    assigned {
      id
      firstName
      lastName
      profileIcon {
        url
        initials
        bgColor
      }
    }
  }
}
